const countTokens = require('./countTokens');
const limitTokens = require('./limitTokens');
const logRequestError = require('./logRequestError');

module.exports = ({
  countTokens,
  limitTokens,
  logRequestError,
});
